{
  "name": "React Native + Django Dev Container",
  "dockerFile": "Dockerfile",
  "forwardPorts": [8000],
  "postCreateCommand": "pip3 install --user -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
